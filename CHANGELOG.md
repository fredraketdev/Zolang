# Changelog

All notable changes to Zolang will be documented in this file.

## [1.0.0] – 2025-12-18 – First Stable Release 🎉
### Added
- Variables (`verander x naar ...`)
- Math & strings (`plus`, `maal`, `"hello"`)
- I/O (`toon`, `vraag`)
- Control flow (`als`/`anders`, `zolang`)
- Comments (`// ...`)
- Type conversion (`getal`, `tekst`)
- Easter egg: `toon.translate`

### Changed
- Project renamed from "own_lang" → **Zolang**
- Variables now allow digits and `_` (`x_1`, `score123`)
- `plus` operator: smart number/string handling

### Fixed
- Nested `als` statements
- Comparisons work outside conditionals
- Error messages now in red with clear feedback
